# Active Steering

Put the task files here that should drive the next unattended coding cycles.

Selection rule:

- the automation reads this directory first
- if multiple files are present, the lexicographically first task file is selected
