# Ownership

Map major areas to the people, agents, or systems that own them.

Examples:
- frontend routes
- backend services
- deployment surfaces
- CI or release workflows

This is useful when an unattended agent needs to know where not to make speculative changes.
